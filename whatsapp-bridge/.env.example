# BRIDGE_API_TOKEN: optional Bearer token for REST API auth.
# All endpoints except /api/health require: Authorization: Bearer <token>
# Leave empty to disable (loopback-only deployment).
BRIDGE_API_TOKEN=
WEBHOOK_URL=
WHATSAPP_BRIDGE_PORT=8080
FORWARD_SELF=true
